[pytest]
pythonpath = .
markers =
    api: API tests
    ui: UI tests
    db: database tests
    smoke: smoke tests
    slow: slow tests
    regression: Regression tests
    movies: Movies tests

addopts = -v